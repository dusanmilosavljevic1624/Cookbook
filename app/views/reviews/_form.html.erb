
    <%= form_for [@recipe, @review], remote: true do |f| %>
        <%= f.label :body %>
        <%= f.text_area :body, class: 'form-control' %>

        <%= f.submit "Submit a review", class: 'btn btn-primary'%>

    <% end %>
